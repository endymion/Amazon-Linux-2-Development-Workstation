name: CDK
description: AWS Cloud Development Kit
schemaVersion: 1.0
phases:
  - name: build
    steps:
      - name: InstallCDK
        action: ExecuteBash
        inputs:
          commands:
            - sudo npm install -g aws-cdk
