name: AWS EFS utilities
description: Elastic File System
schemaVersion: 1.0
phases:
  - name: build
    steps:
      - name: EFSBuildStep
        action: ExecuteBash
        inputs:
          commands:
            - sudo yum install -y amazon-efs-utils
